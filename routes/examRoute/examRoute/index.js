import express from "express";
// import { getExamDetails } from "../../controllers/exam/examController.js";

const router = express.Router();

// /api/category/exam/:slug/details
// router.get("/:slug/details", );

export { router as examRoute };
